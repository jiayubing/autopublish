# 客户生成任务化与并发计划

状态：IN_PROGRESS
基线：`master@48f812255e2d24571ffe72ca9af284812a322270`
分支：`feature/client-generation-tasks`

## 目标

把当前“单篇生成”改造成真正的“客户生成”：当前客户可一次生成多篇文章，使用受控并发执行，页面展示任务进度而不是文章正文预览；客户 A 生成时允许切换到客户 B 并发生成。客户生成和批量生成共享一个全局 AI 执行调度器，默认全局并发上限 4，并保留未来多 AI 模型/供应商资源池的薄边界。

## 边界与产品决策

- 导航改为“问题采集 / 客户生成 / 批量生成”。旧 `single` 仅做一次本地偏好迁移，不继续作为业务概念。
- 客户生成一次 1–100 篇；单客户并发默认 2，可选 1–4。
- 同一客户同一时间只允许一个客户生成作业；不同客户允许同时运行。
- 每篇文章是独立任务：`pending/running/succeeded/failed`；成功立即持久化；失败不回滚成功项；终态失败项可重试。
- 客户生成与批量生成共用全局执行调度器，默认最多 4 个正在进行的 AI 请求；不同作业按组公平取得空闲槽。
- 调度器不感知具体模型；AI 执行器负责创建当前 provider client。未来多模型只替换/扩展 AI 执行资源层，不修改客户/批量生成业务编排。
- 删除客户生成页底部文章预览/编辑区；文章编辑统一回文章库。

## 实施步骤

1. 新增全局生成执行调度器与 AI executor 组合边界；让客户生成与批量生成都经过此入口。
2. 将 `ai-content-service` 的多篇串行循环改为后台客户生成作业 + 任务池；增加查询、订阅和失败重试，并保留旧生成调用的兼容等待入口供现存调用/测试使用。
3. 扩展 IPC/preload/renderer bridge：启动客户作业、读取当前客户作业、重试失败项、接收进度事件。
4. 重构 renderer generation feature，使后台作业不再绑定当前 UI client command 生命周期；切客户只切换观察范围，不中止其他客户作业。
5. 将“单篇生成”改名为“客户生成”，增加并发选择与任务进度，移除正文预览区。
6. 补最低必要回归：调度器全局上限/公平性、同客户互斥与跨客户并行、客户作业局部失败/重试、renderer 名称与进度展示、既有批量生成并发回归。
7. 运行目标测试、renderer build/typecheck、再运行 `npm run ci`；记录结果后提交 PR，不自动合并。

## 验收标准

- 客户生成 4 篇、并发 2 时，实际同时 AI 请求不超过 2，且不是串行 1 篇接 1 篇。
- A 客户作业运行时可启动 B 客户作业；全局同时 AI 请求不超过 4。
- 单篇失败时其他任务继续；已成功文章可在文章库看到；失败项可重试。
- 切换客户或切换页面不会停止后台生成；回来能看到最新进度。
- 客户生成页不再显示文章正文预览/编辑器。
- 批量生成仍保留 1–4 的批次并发选择，并受到同一个全局 AI 并发上限约束。
- AI provider 配置在任一生成 AI 请求运行时仍保持 busy 保护。

## 进度 / 发现 / 验证

- [x] 核实当前 `master` 与现有生成实现：多篇客户生成目前是串行 `for + await`，且 `activeOperation` 为全服务唯一作业，导致跨客户互斥。
- [x] 核实现有批量生成 runner 已具备 1–4 worker、任务状态和局部失败语义，可保留并接入共享 scheduler。
- [ ] 实施代码与测试。
- [ ] 目标测试通过。
- [ ] `npm run ci` 通过。
- [ ] PR 创建并完成交接。

## 2026-09-09 批量生成来源筛选修复

- 本次局部修复基于 `codex/ui-refresh-20260909@ecc4bff2`，工作分支为 `codex/batch-generation-eligibility`；不改变上述历史实施清单。
- 用户要求进入来源检查页前自动排除不满足条件的客户，并解决取消选择后仍无法继续的问题。
- 原因：向导原先把空资料/空调研选择一起交给预览接口，而现有请求合同要求每个来源数组至少一项，导致整份请求被拒绝。客户详情异步读取期间也缺少明确的推进门禁。
- 修复：等待所选客户详情读取完成，在推进向导时按既有来源可用性规则移除本批次中的不合格客户，展示客户与原因；全部排除时留在客户选择页。读取失败保留选择并提示，不当成缺资料。来源手动取消后本地提示补选或移除客户，恢复选择后可继续，避免发送空来源请求。
- 保持 generation service 对正文、来源和启动的最终校验；未改 IPC 合同、后台调度、并发规则或持久化事实。
- 回归覆盖：合格/缺资料/缺回答混选，全部不合格，延迟详情，详情读取失败后取消该客户，取消最后一份资料后重新勾选，以及非当前客户的冷启动读取；合格请求额外经真实 IPC request schema 验证。
- 验证：4 项浏览器场景、1 项非当前客户读取测试、36 项来源/批次服务/IPC/并发测试通过；ESLint、renderer typecheck 和 renderer build 通过，保留已有 bundle-size 警告。最终 bounded review 修正了确认页“下一步”应保持原页的行为，并重跑浏览器场景。未发出真实 AI 请求、未 push/PR/合并。

## 2026-09-09 失败批次结束与结果恢复

- 同一批量生成修复分支上的独立修复。用户报告成功 3、失败 1、取消 7 时，首次结束界面闪回，第二次报通用安全错误。
- 用真实 batch store/runner 和合成生成器复现相同计数：abandon 已持久化，但后续 runtime snapshot 仍读取 runner 残留的 failed，覆盖结束事件。无活动运行时改为读取该批次的持久状态，活动运行的状态所有权不变。
- 取消待处理任务后组件原先长期保留一份本地批次快照；删除该重复快照，统一消费 generation feature 的结果。已结束批次禁用继续和再次结束，保留失败/取消证据以及成功文章的投稿入口。
- 冷启动原先直接隐藏最近 abandoned 批次；现在恢复该批次结果，不恢复执行，便于用户在重启后继续处理已成功文章。
- 回归：68 项批次服务、store、runner、event、feature、IPC 测试以及 5 项浏览器场景通过；覆盖取消后结束、结束错误提示、终态控制禁用、打开投稿对话框无投稿 mutation、重启恢复。ESLint、main/renderer typecheck、renderer build 和 diff check 通过；构建仅有原有 bundle-size 警告。
- 本次未读写真实客户批次、未调用真实 AI、未发起投稿，未 push/PR/合并。修复直接调用链已进行 bounded review，不改变结束合同、文章生命周期、失败证据或后台重试策略。
