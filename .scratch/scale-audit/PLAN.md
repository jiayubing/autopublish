# 规模化架构与性能审计：第一轮

状态：AUDIT_COMPLETE / SCALE_TARGET_BLOCKED。基线1e74583cf0ee2be02125c65298bf3b5579a8bca5。

范围：当前 OperationalStore、文章库、投稿中心、启动恢复及对应 IPC 读取路径；保持现有业务行为，仅增加合成探针与审计证据，不改生产实现。用户列举的旧 aggregation 文件当前已不存在，按真实调用链替换；首页是否存在先核实实际入口。pelican-bicycle.html为用户改动，不触碰。

方法：1千/1万/5万合成任务，分别控制客户数、队列组数及每组长度；真实SQLite与生产查询入口，统计SQL次数、返回行数/字节、重复SQL、执行计划、耗时、事件循环阻塞、服务/IPC字节和失效重读。建立真实小样本准入后复制合成SQL行用于大规模只读压测，明确与真实写入吞吐、文件库端到端测试的区别。单个探针超时有界，不运行真实账号/发布/生产迁移。

结果：每条发现包含严重度、来源、可复现证据、owner和最小改进方向；区分确定问题、容量边界、未测路径。P0为用户审计顺序，不预设发现严重度。完成后只进行证据和已知边界复核，不启动无限全仓审计。

后续独立轮次：投稿执行资源生命周期；第三平台接入成本；publication/OperationalStore依赖方向；历史兼容与CI负担。此轮只登记，不自动重构或退役。

结果见 [AUDIT.md](AUDIT.md)：7项发现（3 P1、4 P2），48项基线回归通过；规模测试复现查询放大、上限截断和启动阻塞。只读审计已完成，未修复生产代码；其余四轮仍待后续安排。实际基线与15份核心源码SHA、探针SHA见source-state.json。
