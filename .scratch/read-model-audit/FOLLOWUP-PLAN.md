# 读模型后续性能优化

状态：COMPLETE。基线 8e270ece；开始时工作树干净。上一轮证据保持历史状态，本轮结果见 FOLLOWUP-RESULTS.md。

1. 发布档案列表用数据库投影去掉正文；完整 evidence 仍由原 owner 校验和按需查询。详情入口校验 client/article identity，不降低永久发布证据合同。
2. 全文搜索按文章分片让出事件循环，保留原匹配和文件一致性检查；避免整客户正文同时常驻。
3. 摘要列表复用同次调用的安全客户目录验证，保留逐文件版本/符号链接检查，不用失效不明的整列表缓存。
4. 媒体资源使用现有 store 的版本快照，在分页前避免整库深拷贝与反复规范化；保持调用方隔离与文件变更可见。
5. 定向行为、SQL/文件载荷/事件循环测量、UI加载/失败/迟到响应验证；Primary Review 后仅针对 findings 有界复审，最终完整桌面及工具链验证。

不改持久 schema，不删除发布证据，不执行真实外部操作；未授权 commit/push。阅读集：根入口、应用规则、上轮剩余边界、相关 owner/直接消费者/测试和生命周期审计协议。

## 最终结果

- 四项优化按范围完成，Primary Review / bounded re-review PASS。
- 最终桌面 1760/1760，无 fail/skip/cancel/todo；打包合同49、迁移68、IPC矩阵6均通过；lint/typecheck/build/format通过。
- 已发布1000篇快照从10.16MB降为6.05MB；首次查询中位约1.18秒，刷新约0.43秒。1000篇全文搜索仍约1.94秒总耗时，但定时器最大采样间隔约11.6ms。
- 安全边界：完整详情仍验正文指纹，摘要不是发布证据 writer 输入；目录被替换则失败；分页返回副本；无远端操作。
- 未提交/推送；最终源码与测试哈希见 followup-source-state.json。最终完整测试之后未修改 production source 或测试。
