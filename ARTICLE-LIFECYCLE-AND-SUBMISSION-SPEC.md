# 文章库与投稿中心产品规格

状态：2026-08-15 产品重设计已确认。本文取代旧的“六个互斥文章入口”导航模型；既有远端副作用、单活动目标、不确定结果、订单保留和发布成功不可逆规则继续有效。

## 1. 目标

AutoPublish 使用一条清楚、可恢复、不会重复投稿的主链路：

`内容生产 → 文章库 → 发起投稿 → 投稿中心 → 发布档案/订单`

用户不需要理解数据库、发布尝试、恢复事务或多个内部状态机。界面只回答：

1. 文章现在能否编辑和投稿；
2. 投稿任务正在等待、执行还是需要核对；
3. 哪个动作安全、会产生什么结果；
4. 是否可能已经发生远端投稿或付费订单。

模块必须让文章正文、普通平台队列、付费批次、服务商订单、发布档案和人工待办各有唯一 owner，不通过一个万能“文章状态”覆盖所有事实。

## 2. 核心决策

### 2.1 文章与投稿任务分离

- 文章保存内容事实：身份、客户、标题、正文、生成来源和必要快照。
- 普通平台队列项、已确认付费批次、发布尝试、服务商订单和需处理事项是独立运行事实。
- 文章库按这些事实生成只读分类和操作权限，不写入持久化 `articleStatus` 作为业务真源。
- `付费处理中` 是服务商订单状态，不再是文章导航入口。
- `需处理事项` 是人工待办，不再是文章阶段；同一文章可关联多项待办。

### 2.2 生成只生产文章

- 单篇或批量生成成功后立即保存文章。
- 完整文章进入文章库的“待投稿”；内容不完整时进入“待完善”。
- 生成页不直接创建普通平台队列、付费批次或活动发布目标。
- 批量生成结束后只提供“查看本批次文章”，导航到文章库并按本批次筛选；用户随后通过统一“发起投稿”操作选择渠道和目标。

### 2.3 只有确定目标后才冻结

- 普通平台必须同时确定平台和平台账号档案，确认入队时原子建立队列项与活动目标并冻结文章。
- 付费媒体必须先确定媒体资源、重新获取接单状态和价格、完成费用确认，之后才原子建立已确认付费批次与活动目标并冻结文章。
- 临时勾选、打开投稿面板、选择渠道和付费预检都不建立持久业务队列，不冻结文章。
- 删除“付费媒体投稿暂存队列”作为持久业务概念；确认前的选择只属于当前投稿操作会话。

### 2.4 删除不暗中改变投稿

- “移出队列”和“移入回收站”是两个独立动作。
- 有普通平台队列项、已确认付费批次、活动订单、远端结果不确定或发布成功事实时，不允许直接移入回收站。
- 用户必须先在投稿中心结束可安全结束的任务，再回文章库移入回收站。
- 第一阶段不提供“移出队列并删除”的组合按钮，防止一个动作同时改变两个 owner。

## 3. 用户工作区

### 3.1 内容生产

包含客户资料、问题采集、单篇生成和批量生成。

职责：

- 收集生成输入；
- 创建和恢复生成批次；
- 保存成功文章；
- 展示任务级失败、暂停、继续和取消。

不负责：

- 选择投稿平台或媒体；
- 创建投稿队列；
- 执行远端发布；
- 处理订单和远端不确定结果。

### 3.2 文章库

统一查看和管理文章内容。默认按当前客户展示，并支持批次、标题、模板和创建时间筛选。

文章库只读分类：

| 分类 | 含义 |
| --- | --- |
| 待投稿 | 标题正文完整、可编辑、没有活动目标、没有不确定结果、未发布、未回收 |
| 待完善 | 内容不完整，或存在必须先编辑才能投稿的内容问题 |
| 投稿中 | 有普通平台队列项、已确认付费批次、活动服务商订单或待核对远端结果 |
| 已发布 | 已有首次可信发布成功事实，永久只读 |
| 回收站 | 未发布且已安全结束所有运行事实，可恢复或永久删除正文 |

文章卡片可显示渠道、目标、最近结果和需处理数量，但这些标签不成为第二套状态 owner。

文章库允许的主要动作：

- 打开和保存可编辑文章；
- 对待投稿文章发起投稿；
- 查看投稿进度、发布档案或订单；
- 对安全文章移入回收站；
- 恢复回收站文章；
- 在二次确认后永久删除回收站正文。

### 3.3 投稿中心

投稿中心集中承载投稿任务，不再把操作分散到文章管理、普通平台页和付费媒体页。

提供三个入口：

1. `普通平台队列`：分组、顺序、当前文章、开始、暂停和移出未开始项；
2. `付费投稿批次`：已确认费用的批次、剩余文章、订单创建进度、暂停和取消剩余未开始项；
3. `需处理事项`：按问题类型给出安全的人工动作。

普通平台和付费媒体使用不同执行模块，但共享文章身份、活动目标排他、文章冻结和只读工作台投影。

### 3.4 订单

订单页只处理已经真实获得订单号的服务商订单：

- 待安排、已安排、已发布、已退稿、售后中和全部筛选；
- 单个或全部刷新；
- 取消、尝试取消、申诉和人工状态核对；
- 查看媒体、价格、系统投稿标识、提交快照和远端链接。

没有订单号的订单创建不确定结果属于投稿中心的需处理事项，不伪造服务商订单。

## 4. 发起投稿

### 4.1 通用入口

用户在文章库勾选一篇或多篇待投稿文章，点击“发起投稿”。系统创建一个仅用于当前操作的投稿选择会话，不写入队列或活动目标。

投稿面板必须显示：

- 文章数量和标题；
- 不可投稿文章及稳定原因；
- 渠道选择：普通平台或付费媒体；
- 当前文章内容是否有未保存修改。

任何确认都必须基于文章锁内重新读取的权威正文和指纹；Renderer 勾选和预检结果不是最终授权。

### 4.2 普通平台

用户选择一个普通平台和一个平台账号档案。确认后系统原子执行：

1. 重读文章与生命周期事实；
2. 验证全部文章仍可投稿；
3. 建立普通平台队列项、不可变投稿快照和唯一活动目标；
4. 冻结文章；
5. 将文章追加到该平台账号队列尾部。

一次确认只能进入一个平台账号队列组。不同平台可并行；同组 FIFO；当前共享平台会话约束下，同平台不同账号组继续串行。

### 4.3 付费媒体

用户选择一个媒体资源后，系统执行只读费用预检：

- 重新获取媒体接单状态、价格和备注；
- 验证标题、正文、系统投稿标识码和内容风险；
- 展示文章数、单价、预计总价和风险提示；
- 生成短期有效、绑定文章指纹与资源指纹的确认凭据。

用户明确确认费用后，系统再次查询资源并在锁内复核文章，然后原子建立已确认付费批次、不可变订单提交快照和每篇文章的唯一活动目标。确认成功前没有持久“付费队列”。

已确认付费批次不可追加文章，也不可单独移除文章，因为文章集合属于费用确认事实。用户可以暂停批次，并在没有在途请求时取消全部剩余未开始项；已创建订单和在途请求不受影响。取消剩余项后，相关未开始文章在没有其他阻塞事实时恢复待投稿；重新提交必须重新预检和确认费用。

## 5. 普通平台队列

### 5.1 队列行为

- 分组身份为普通平台和平台账号档案；
- 同组严格 FIFO；
- 新文章可追加到队尾；
- 应用启动后所有组保持暂停；
- “开始全部”只启动未开始且非人工暂停的组；
- “暂停全部”只阻止下一篇，不伪造中断远端请求；
- 一个组的完成、失败或暂停不终止其他平台。

### 5.2 移出队列

- 只有状态仍为等待且远端调用尚未开始的队列项可以移出；
- 移出操作原子删除队列项并结束对应活动目标；
- 文章在没有其他阻塞事实时恢复待投稿和编辑；
- 已领取、准备提交、远端调用已开始、结果不确定或已发布的项不能移出；
- 移出不会把文章放入回收站，也不会删除历史证据。

### 5.3 投稿结果

| 远端结果 | 本地结果 |
| --- | --- |
| 明确接受 | 建立全局发布成功，文章永久只读 |
| 文章级明确拒绝 | 结束当前目标，文章恢复可编辑；建立明确失败待办 |
| 平台、认证或系统级失败 | 暂停对应组；当前项按明确结果保持或结束，其他平台继续 |
| 结果不确定 | 冻结文章、暂停对应组并建立待核对事项；禁止自动重试 |

普通平台明确接受即算发布成功，不增加公开页面轮询或审核等待。

## 6. 付费投稿批次与订单

- 已确认付费批次全局串行创建订单；
- 每笔订单前重新核对媒体、价格、文章指纹和系统投稿标识；
- 远端调用前持久化稳定 attempt 和 `remote_call_started` 边界；
- 只有明确成功且获得订单号才建立服务商订单；
- 订单创建成功后批次项结束，文章进入订单跟踪；
- 文章或媒体级明确拒绝结束当前目标并恢复文章可编辑；
- 余额、账号或服务级明确拒绝暂停剩余批次；
- 超时、断线、协议异常或成功响应缺少订单号一律是不确定结果，禁止自动重试；
- 重启后批次保持暂停，不自动继续产生费用。

服务商订单状态仍映射为：`0 待安排`、`1 已安排`、`2 已发布`、`4 已退稿`、`9 售后中`。

首次可信状态 2 建立全局发布成功。之后的售后、退款、链接失效或未收录不撤销文章已发布事实。

## 7. 需处理事项

需处理事项必须有稳定身份、明确类型、所属 owner、安全摘要、文章冻结信息和有限动作。禁止根据错误文本决定业务动作。

| 类型 | 文章权限 | 允许动作 |
| --- | --- | --- |
| 普通平台明确失败 | 无其他阻塞时可编辑 | 打开文章、重新发起投稿、查看证据、移入回收站 |
| 普通平台结果不确定 | 冻结 | 确认已接受、确认未接受、查看实际提交证据 |
| 付费订单创建不确定 | 冻结 | 验证并补录订单号、确认服务商没有订单、查看证据 |
| 已知订单状态异常 | 冻结 | 恢复订单跟踪、确认已发布、确认明确非发布终态 |
| 删除事务需要修复 | 依真实事实决定 | 检查差异、继续或重试本地删除修复 |
| 发布档案归档失败 | 已发布且只读 | 重试本地归档、查看发布证据 |

规则：

- 不确定结果永远没有“直接重试投稿/下单”；
- 明确失败的再次投稿必须回到统一发起投稿入口，不建立专用 retry 状态机；
- 一项事实解决后对应待办自动关闭；
- UI 可以按文章聚合展示多项待办，但不得合并身份、原因或 resolution；
- 系统修复事项不得暴露远端投稿动作。

## 8. 删除与回收站

### 8.1 移入回收站

仅当文章满足以下全部条件时允许：

- 没有可信发布成功；
- 没有普通平台队列项或已确认付费批次；
- 没有活动服务商订单；
- 没有在途远端请求或不确定结果；
- 没有开放的删除事务冲突。

删除预检只报告阻塞事实，不自动撤销队列、取消订单或结束目标。

### 8.2 恢复

- 恢复正文和文章身份；
- 不恢复普通平台队列、付费批次或活动目标；
- 恢复后根据正文完整性进入待投稿或待完善；
- 已永久删除正文的文章不可恢复。

### 8.3 永久删除

- 仅从回收站发起并需要短期确认凭据；
- 删除可编辑正文和非必要来源数据；
- 永久保留墓碑、标题、客户安全快照、目标、订单、金额、时间、结果和必要发布证据；
- 不提供订单或发布档案删除命令。

## 9. 深模块与 owner

### 9.1 内容生成模块

拥有生成批次、任务恢复、AI 输入和成功文章创建。公开接口只覆盖预检、启动、暂停、继续、取消和读取批次；不依赖投稿队列或订单能力。

### 9.2 文章内容与变更协调模块

拥有文章文件、CAS 保存、规范文章锁顺序、锁内事实重读、回收/恢复/永久删除协调和 release failure 的不确定映射。调用方不能取得锁、session、文件事务或完整 OperationalStore。

### 9.3 文章生命周期投影

纯只读地计算文章库分类、编辑权限、投稿资格、阻塞原因和安全操作。所有页面和命令共享这一 owner，不复制判断。

### 9.4 普通平台队列模块

拥有普通队列 admission、FIFO、claim、准备/提交边界、开始/暂停、移出未开始项和普通平台结果分类。平台 adapter 只返回 `accepted | article_rejected | group_blocked | uncertain`，不拥有文章冻结和人工核对。

### 9.5 付费投稿模块

拥有媒体预检、费用确认、已确认批次、串行订单创建、暂停、取消剩余项和订单创建结果。供应商 adapter 不拥有批次、文章权限或重试规则。

### 9.6 需处理中心

拥有待办查询、动作策略、stale 防护和具名 resolution 路由。它调用各真实 owner 的窄 capability，不直接写队列、订单、文章或发布事实。

### 9.7 投稿工作台只读模型

一次性组合队列组、付费批次、订单摘要、需处理事项和文章安全摘要。它不拥有状态转换；Renderer 不再自行拼接多个 store 推断可用动作。

### 9.8 Transport 与 Renderer

IPC、preload、bridge 和 component 只做协议映射、展示和收集用户意图。业务应用接口应少量、具名且版本化；允许 transport adapter 保持薄，但禁止建立多层纯透传 service/manager/coordinator 链。

## 10. 迁移规则

- 六分类旧 UI 没有独立持久状态，不迁移文章文件；由新投影重新分类。
- 既有普通平台队列、已确认付费批次、订单、不确定结果和发布档案原样保留。
- 旧 `paid_staging_items` 不代表活动目标或远端副作用。正式 migration 必须原子记录受影响文章身份和迁移摘要，然后清除这些暂存行；文章保持待投稿，并向用户显示一次“请重新发起付费投稿”的安全通知。
- 旧生成批次投稿交接 capability 在生成页消费者迁移后删除，不保留 compatibility re-export。
- 迁移不得创建新队列、订单、发布尝试或远端请求。

## 11. 验收矩阵

至少覆盖：

1. 生成成功只创建文章，不创建任何投稿事实；
2. 批次完成后导航并筛选本批次文章；
3. 普通平台确认前文章可编辑，确认后立即冻结；
4. 付费预检前后均不冻结，费用确认成功后才冻结；
5. 旧付费暂存不能让文章在界面显示可投稿但命令被隐藏冲突阻止；
6. 普通平台未开始项移出后恢复待投稿，不进入回收站；
7. 有队列的文章删除预检明确阻止且不修改队列；
8. 明确失败可编辑，不确定结果继续冻结；
9. 普通不确定只允许两种人工确认；付费不确定只允许补录订单或确认无订单；
10. 已确认付费批次不能追加或单独移除，取消剩余项不影响订单和在途请求；
11. 已发布高于退稿、售后、退款和迟到 observation；
12. 回收、恢复和永久删除不删除订单或发布证据；
13. 多文章 admission、删除、取消和保存覆盖反序集合、并发、重复调用、崩溃和 stale input；
14. Renderer 覆盖空态、加载、错误、禁用、确认、窄屏和关键跨页导航；
15. 性能验证批量读取，不按文章逐项查询运行事实。

自动化测试只使用合成数据和假 transport。真实登录、发布、付费、取消或生产迁移必须另行获得本次明确授权。

## 12. 非目标

- 同一文章并发投向多个目标；
- 生成完成后自动投稿；
- 审核、批量审核或“待审核/已审核”兼容路线；
- 不确定远端结果自动重试；
- 普通平台公开页面轮询或审核等待；
- 已发布文章再次投稿、回收或删除发布证据；
- 删除操作自动取消队列、订单或远端请求；
- 通过万能工作流引擎统一普通平台和付费媒体的不同副作用语义；
- 为旧付费暂存或旧生成交接长期保留 adapter、alias 或 compatibility layer；
- 未经真实授权的图片上传、真实付费或生产发布。
