# 文章生命周期与投稿流程规格

状态：业务规则已确认，可进入任务拆分；当前核心里程碑以纯文本链路验收，普通平台图片扩展后置，网站媒体图片传输保留独立待实测事实。

## Problem Statement

当前文章管理、普通平台投稿和网站媒体订单沿用了多套相互冲突的旧概念：文章需要审核才能入队、同一篇文章可同时投向多个目标、队列保存独立内容副本、普通平台存在“已提交但未发布”的中间状态、网站媒体与普通平台共享不合适的执行语义。旧文档还允许已发布文章回收，并把用户界面分类直接绑定到底层状态。这些规则增加了操作步骤，使文章能否编辑、何时算发布成功、付费订单如何处理以及异常后能否重试都难以判断。

用户需要一套面向 GEO 批量铺设的简单流程：生成文章直接待投稿；文章一旦入队即冻结；每篇文章同时只有一个活动目标；普通平台接受即算已发布；网站媒体按服务商订单状态跟踪；任何不确定结果都禁止自动重试；所有分类由真实业务事实统一派生，后续调整标签时不需要迁移历史文章。

## Solution

系统以文章为中心提供六个互斥入口：`待投稿`、`投稿队列`、`付费处理中`、`需处理`、`已发布`、`回收站`。这些名称是由文章、队列、发布尝试和服务商订单事实计算得到的视图，不作为独立持久化状态写入文章。

文章成功生成并保存后直接进入“待投稿”，不再经过审核。文章进入任意投稿队列后立即冻结；尚未开始的队列项被明确移除后恢复编辑。一篇文章同时只能有一个活动发布目标，目标明确失败或被取消后才可以改投其他目标。一旦普通平台明确接受投稿，或网站媒体订单状态变为“已发布”，文章即全局进入“已发布”、永久冻结并从活动列表移除。

普通平台按照“平台 + 平台账号档案”形成独立队列组。不同平台可以并行，同一组内严格按先进先出顺序执行。当前核心阶段尚未建立账号专属浏览器会话，因此同一平台的多个账号档案组共享平台级执行锁并严格串行；账号专属会话与同平台多账号并行后置扩展。某个平台完成不会终止其他平台。普通平台只要明确接受投稿就算发布成功，不等待公开页面，不轮询后续审核结果。

网站媒体是通过当前服务商 API 创建付费订单的独立渠道，不接入“第三方自媒体”。订单创建成功后，文章立即离开投稿队列并进入“付费处理中”；订单的详细状态、取消、申诉、退款和刷新统一在订单页处理。付费任务不会被普通平台的“开始全部”或“暂停全部”控制，任何实际下单都必须经过单独的费用确认。

### 里程碑边界

当前核心地基里程碑只完成并验收文章生命周期、纯文本普通平台投稿、网站媒体订单、迁移、删除与交付门禁。核心完成不代表图片生产链已经实现：Ticket 18 在核心验收后先完成通用图片准备；随后必须对列举网、今日头条、蓝色河畔逐平台经用户单独明确授权执行真实能力探索，仅对结论为 `SUPPORTED` 的平台实施对应 adapter，并在实施后另行获得该平台真实带图验收授权。探索结论为 `UNSUPPORTED` / `INCONCLUSIVE` 时该平台图片入口保持关闭，但合规探索证据不得让整个图片里程碑永久死锁。核心阶段只固定封闭 evidence 扩展接缝，实际生产 evidence 必须使用 `deliveryMode=text_only`、空图片清单和初始决定。不可用的图片入口不得出现在生产 UI。

网站媒体图片与 Ticket 18–21 无关，继续保持未实现、未承诺。只有用户明确授权真实低价媒体探索，并预先限定费用、格式、尺寸、数量、请求体、传输机制和停止条件后，才能收集事实；探索结果仅可为 `SUPPORTED`、`UNSUPPORTED` 或 `INCONCLUSIVE`。`SUPPORTED` 只允许另建实施 ticket，`UNSUPPORTED`/`INCONCLUSIVE` 均保持纯文本且不阻塞核心完成。真实验证前不得采用自建 OSS、外部图片托管或另一个系统的私有 UEditor 接口。

### 文章分类规则

| 界面入口   | 用户看到的业务含义                                                                 |
| ---------- | ---------------------------------------------------------------------------------- |
| 待投稿     | 标题和正文完整，且没有活动队列、活动订单、不确定结果或发布成功事实                 |
| 投稿队列   | 文章正在等待普通平台投稿，或等待创建网站媒体订单                                   |
| 付费处理中 | 已有真实服务商订单，订单仍为待安排或已安排                                         |
| 需处理     | 明确失败、网站媒体退稿、结果不确定、订单状态异常或删除恢复异常，需要人工决定下一步 |
| 已发布     | 普通平台已接受投稿，或网站媒体订单已发布；文章只读并永久保留                       |
| 回收站     | 尚未成功发布且没有活动队列、订单或不确定结果的文章被用户移除后形成的可恢复内容     |

### 普通平台主要转换

| 当前事实               | 结果                                                   |
| ---------------------- | ------------------------------------------------------ |
| 待投稿文章入队         | 进入投稿队列并冻结                                     |
| 尚未开始的队列项被移除 | 回到待投稿并恢复编辑                                   |
| 平台明确接受投稿       | 进入已发布并永久冻结                                   |
| 文章级明确失败         | 进入需处理并恢复编辑；该平台继续下一篇                 |
| 平台、账号或系统级失败 | 暂停该平台队列；其他平台继续                           |
| 结果不确定             | 当前文章进入需处理并冻结，同时暂停该平台；禁止直接重试 |
| 人工确认平台已接受     | 进入已发布                                             |
| 人工确认平台未接受     | 回到待投稿并恢复编辑                                   |

### 网站媒体主要转换

| 当前事实                     | 结果                                                 |
| ---------------------------- | ---------------------------------------------------- |
| 待投稿文章加入网站媒体任务   | 进入投稿队列并冻结                                   |
| 服务商明确拒绝创建文章级订单 | 当前文章进入需处理并恢复编辑                         |
| 余额、账号或服务级问题       | 暂停剩余付费提交；当前及未开始文章留在队列           |
| 创建订单成功并取得订单号     | 从投稿队列移除，进入付费处理中                       |
| 创建订单结果不确定           | 进入需处理并冻结；禁止自动重试                       |
| 订单状态为待安排或已安排     | 保持付费处理中                                       |
| 订单状态为已发布             | 进入已发布并永久冻结                                 |
| 订单状态为已退稿             | 进入需处理并恢复编辑，可修改、换媒体资源或移入回收站 |
| 服务商明确取消成功           | 保留订单历史，文章回到待投稿并恢复编辑               |
| 订单状态查询异常或订单缺失   | 进入需处理并冻结，等待人工核对                       |
| 已发布后进入售后或退款       | 文章仍保持已发布；售后事实仅在订单页更新             |

## User Stories

1. As an 内容运营者, I want AI 成功生成的文章自动保存为待投稿, so that 我无需逐篇执行审核确认。
2. As an 内容运营者, I want 手工修改后明确点击保存, so that 未确认的编辑不会被误认为已保存。
3. As an 内容运营者, I want 界面不再出现待审核、已审核和批量审核, so that 投稿流程符合 GEO 批量铺设的实际目标。
4. As an 内容运营者, I want 没有 AI 来源的手工文章也能投稿, so that 来源追溯不会阻止有效内容使用。
5. As an 内容运营者, I want 标题和正文作为投稿必需内容, so that 每个目标都能获得可用文章。
6. As an 内容运营者, I want 图片保持可选, so that 没有客户图片时仍可发布文本文章。
7. As an 内容运营者, I want 文章首次进入队列后立即禁止编辑, so that 等待提交的内容不会发生漂移。
8. As an 内容运营者, I want 从暂停队列移除尚未开始的文章, so that 少量需要修改的文章可以恢复编辑。
9. As an 内容运营者, I want 批量移除尚未开始的队列文章, so that 我能快速调整投稿计划。
10. As an 内容运营者, I want 正在提交、已有订单或结果不确定的文章继续冻结, so that 不会产生重复远端结果。
11. As an 内容运营者, I want 一篇文章同时只有一个活动发布目标, so that 文章状态始终明确。
12. As an 内容运营者, I want 旧目标明确结束后再选择新目标, so that 改投不会与旧任务并发。
13. As an 内容运营者, I want 任意目标成功后文章永久归档, so that 已发布文章不会再次被误投。
14. As an 内容运营者, I want 不提供复制为新版本或复制为新文章, so that 低频功能不会增加界面负担。
15. As an 内容运营者, I want 六个互斥的文章入口, so that 每篇文章只出现在一个清晰位置。
16. As an 内容运营者, I want 分类由真实事实自动计算, so that 将来调整标签不需要迁移历史数据。
17. As an 内容运营者, I want 已发布文章保留完整投稿正文并只读展示, so that 我能查看历史内容而不会误改。
18. As an 内容运营者, I want 已发布列表显示标题、客户、目标、时间和结果, so that 我能快速核对发布记录。
19. As an 内容运营者, I want 已发布文章不能进入回收站, so that 发布档案不会被误删。
20. As an 内容运营者, I want 未发布文章可进入回收站并恢复, so that 删除操作可撤销。
21. As an 内容运营者, I want 永久删除文章后仍保留付费订单凭据, so that 费用、退款和售后记录不丢失。
22. As an 内容运营者, I want 从待投稿批量选择文章并一次只选择一个普通平台与账号, so that 每次入队目标明确。
23. As an 内容运营者, I want 普通平台队列按平台和账号分组, so that 不同登录身份不会混用。
24. As an 内容运营者, I want 只有一个账号时隐藏账号层, so that 日常页面保持简洁。
25. As an 内容运营者, I want 不同普通平台并行运行, so that 一个网站完成或暂停不会阻塞其他网站。
26. As an 内容运营者, I want 同一平台账号内按先进先出执行, so that 投稿顺序稳定且容易理解。
27. As an 内容运营者, I want 队列只显示平台总体状态、当前文章和剩余顺序, so that 页面不会为每篇等待文章重复显示状态。
28. As an 内容运营者, I want 向运行中的普通平台队列追加文章, so that 新文章可以排到队尾继续处理。
29. As an 内容运营者, I want 追加文章继承当前账号和配图数量, so that 不需要重复配置同一队列。
30. As an 内容运营者, I want 使用开始全部启动未开始的普通平台, so that 可以一次启动多个独立平台。
31. As an 内容运营者, I want 开始全部不恢复我手动暂停的平台, so that 手工暂停意图不会被覆盖。
32. As an 内容运营者, I want 使用暂停全部暂停普通平台, so that 可以统一停止后续普通投稿。
33. As an 内容运营者, I want 应用重启后所有普通平台保持暂停, so that 系统不会在无人确认时自动投稿。
34. As an 内容运营者, I want 普通平台明确接受后立即标记已发布, so that 不需要等待无法可靠检测的公开审核结果。
35. As an 内容运营者, I want 普通平台文章级失败不停止同组后续文章, so that 单篇问题不会拖垮整个平台任务。
36. As an 内容运营者, I want 平台、认证或系统故障只暂停受影响平台, so that 其他平台可以继续运行。
37. As an 内容运营者, I want 不确定结果暂停对应平台并冻结文章, so that 系统不会自动制造重复投稿。
38. As an 内容运营者, I want 对普通平台不确定结果只能确认已接受或未接受, so that 人工处理结果明确且不能直接重试。
39. As an 内容运营者, I want 网站媒体与第三方自媒体严格区分, so that 未接入渠道不会进入设计和界面。
40. As an 内容运营者, I want 网站媒体订单必须单独确认费用, so that 普通平台的开始全部不会产生付费订单。
41. As an 内容运营者, I want 付费确认显示文章数、媒体、最新价格、预计费用和系统投稿标识码, so that 扣费前信息完整。
42. As an 内容运营者, I want 媒体停止接单时阻止创建订单, so that 不会向不可用资源提交。
43. As an 内容运营者, I want 价格发生变化时重新确认, so that 系统不会自动接受新的价格。
44. As an 内容运营者, I want 网站媒体标题在下单前限制为 30 个字符, so that 降低服务商退回风险。
45. As an 内容运营者, I want 下单前看到手机号和网址等内容风险提示, so that 我能结合媒体备注决定是否继续。
46. As an 内容运营者, I want 风险提示不自动删除正文或品牌名, so that 系统不会擅自改变文章含义。
47. As an 内容运营者, I want 系统投稿标识码全局统一, so that 我能在服务商处筛出所有由本系统创建的订单。
48. As an 内容运营者, I want 未配置系统投稿标识码时禁止下单, so that 不会产生无法统一检索的订单。
49. As an 内容运营者, I want 网站媒体付费批次严格串行, so that 我能在异常时及时停止后续扣费。
50. As an 内容运营者, I want 新文章不能追加进已确认的付费批次, so that 未确认费用的文章不会自动下单。
51. As an 内容运营者, I want 网站媒体拥有独立的暂停后续提交, so that 已创建订单不受影响而剩余订单停止创建。
52. As an 内容运营者, I want 应用重启后不自动继续付费批次, so that 崩溃恢复不会产生意外费用。
53. As an 内容运营者, I want 只有服务商返回订单号后才建立订单记录, so that 本地订单对应真实远端凭据。
54. As an 内容运营者, I want 订单创建结果不确定时禁止自动重试, so that 网络故障不会产生重复付费订单。
55. As an 内容运营者, I want 查到订单后手动补录订单号, so that 不确定任务可以恢复为正常订单跟踪。
56. As an 内容运营者, I want 补录订单号前自动核对服务商信息, so that 文章不会绑定错误订单。
57. As an 内容运营者, I want 确认没有订单后解除文章冻结, so that 可以安全重新投稿。
58. As an 内容运营者, I want 余额或账号问题暂停剩余付费提交, so that 没有订单的文章仍留在队列等待恢复。
59. As an 内容运营者, I want 单篇或媒体资源问题只影响当前文章, so that 其他可提交文章能够继续。
60. As an 内容运营者, I want 订单创建成功后文章自动离开投稿队列, so that 新文章入队时不会误判旧文章仍待投稿。
61. As an 内容运营者, I want 文章管理只显示付费处理中和查看订单, so that 详细订单状态集中在订单页。
62. As an 内容运营者, I want 订单页默认打开待安排, so that 日常首先看到需要服务商处理的订单。
63. As an 内容运营者, I want 按待安排、已安排、已发布、已退稿、售后中和全部筛选订单, so that 不同阶段清晰分离。
64. As an 内容运营者, I want 打开订单页自动刷新一次并能手工刷新全部, so that 状态及时且仍可主动核对。
65. As an 内容运营者, I want 在订单详情刷新单个订单, so that 排查时不必刷新全部历史。
66. As an 内容运营者, I want 刷新失败不改变原状态, so that 临时网络故障不会伪造订单结果。
67. As an 内容运营者, I want 长期待安排或已安排只显示延迟提醒, so that 平均发布时间不会被误当作失败期限。
68. As an 内容运营者, I want 待安排订单可以取消, so that 服务商处理前可以撤回。
69. As an 内容运营者, I want 已安排订单显示尝试取消并提示可能失败, so that 我仍有机会撤单但不会被误导。
70. As an 内容运营者, I want 服务商明确取消成功后文章恢复待投稿, so that 未发布内容可以再次编辑使用。
71. As an 内容运营者, I want 服务商拒绝取消时保持付费处理中, so that 本地状态不会与远端事实分离。
72. As an 内容运营者, I want 已退稿文章进入需处理并恢复编辑, so that 可以修改、换媒体资源或删除。
73. As an 内容运营者, I want 已发布后的售后和退款不解除文章冻结, so that 曾公开过的内容不会再次投稿。
74. As an 内容运营者, I want 历史订单保存下单时的媒体和价格快照, so that 媒体列表变化不会改写历史费用。
75. As an 内容运营者, I want 订单历史不可删除, so that 费用、退款和售后证据永久可查。
76. As an 内容运营者, I want 已发布档案显示实际投稿正文而不是声称媒体最终正文完全一致, so that 服务商不通知的改稿不会污染本地事实。
77. As an 内容运营者, I want 不同文章即使内容相似也能分别投稿, so that GEO 批量铺设不被相似度规则阻止。
78. As an 内容运营者, I want 每个普通平台任务统一设置 0 至 5 张随机图片, so that 不必逐篇选择图片。
79. As an 内容运营者, I want 每个网站媒体资源统一设置 0 至 5 张随机图片, so that 付费批次的配图操作保持简单。
80. As an 内容运营者, I want 图片只从文章所属客户的本地图片库选择, so that 不同客户的素材不会混用。
81. As an 内容运营者, I want 同一篇文章内图片不重复而不同文章可复用, so that 图片使用既自然又不消耗过多素材。
82. As an 内容运营者, I want 图片不足时使用实际数量且无图时发布纯文本, so that 图片库规模不会阻塞文字投稿。
83. As an 内容运营者, I want 图片由各平台按自身真实图文能力自动放置, so that 我不需要逐篇手工决定图片位置。
84. As an 内容运营者, I want 任意图片不足、读取或上传失败都自动减少图片数量直至纯文本, so that 可选图片永远不会阻塞 GEO 文字投稿。
85. As an 内容运营者, I want 网站媒体最终未显示图片时仍将正文成功视为已发布, so that 可选图片不会推翻文本发布结果。

## Implementation Decisions

- 文章分类是单一、集中、只读的派生能力。所有页面、导航数量和操作权限使用同一个分类结果，不得各自复制判断规则；调整标签名称或归类规则不要求改写历史文章。
- 文章持久化保存稳定身份、客户、标题、正文、生成来源和必要快照；审核状态不再属于新文章模型。生成成功即保存，后续手工修改仍需明确保存。
- 投稿资格只依赖客户归属、非空标题、非空正文以及不存在阻塞性运行事实。生成资料、调研回答和模板来源用于追溯但不构成硬门槛。
- 编辑权限由统一策略判断：任何等待队列、执行中请求、活动网站媒体订单、不确定结果或发布成功事实都会冻结文章。明确取消、明确失败或明确退稿且不存在其他阻塞事实时恢复编辑。
- 一篇文章最多拥有一个活动发布目标。活动目标结束且未成功后可以改投；首次成功建立全局发布事实，永久阻止再次入队、改投和回收。
- 投稿队列项是文章与目标之间的待执行任务，不是独立文章副本。执行时允许保存不可变投稿快照作为审计证据，但不向用户暴露“文章版本”或“队列副本”。
- 普通平台队列以平台和平台账号档案作为分组身份；不同平台可并行，同组串行 FIFO。当前核心阶段同一平台的多个账号档案组由平台级执行锁串行，只有核验当前登录账号与目标档案一致的组才能准备投稿；账号专属会话与同平台多账号并行后置扩展。只有一个账号时界面隐藏账号层，但内部目标身份仍包含账号。
- 普通平台 attempt 先以 `prepared` 持久化。平台阶段一产生仅进程内 `PreparedSubmission` capability，同时提供安全平台无关 `preparedSubmissionEvidenceV1`，记录实际标题/正文/content fingerprint、delivery mode 和实际成功图片的安全 fingerprint/布局；既有 `decisionKind` 仅作 V1 兼容，新自动配图路径固定 `initial`。executor 在最终提交紧前通过一个事务冻结该 evidence 并写入一次 `remote_call_started`；capability 私有会话不持久化。该标记是禁止安全自动重放的保守本地边界：标记前可在用户重新开始并复核后重做准备，标记后即使进程内 capability 消失，人工核对仍只能使用已冻结的实际提交 evidence。
- 普通平台的远端明确接受响应直接产生全局已发布事实。系统不新增公开页面轮询、审核等待或后续可见性判定。
- 普通平台文章级失败继续下一篇；平台、认证或系统级失败暂停对应组；不确定结果同时冻结文章并暂停对应组。其他组始终独立运行。
- 普通平台不确定结果只允许“确认已接受”和“确认未接受”两种人工收口，不提供直接重试。
- 网站媒体与普通平台使用不同应用服务和执行策略，共享文章身份、编辑权限与分类投影。第三方自媒体不注册为当前渠道。
- 网站媒体投稿队列按媒体资源组织，但任何批次都必须单独付费确认。普通平台的开始全部和暂停全部不触发或控制网站媒体。
- 付费确认前重新获取媒体可用状态和价格；不可用时阻止，价格变化时使原确认失效并要求重新确认。批次运行中每个订单提交前仍需复核，变化后暂停未提交项。
- 网站媒体标题在付费预检时限制为 30 个字符。手机号和网址采用本地快速风险提示，不调用 AI、不持续扫描，也不自动改写正文；媒体备注与风险一并展示。
- 网站媒体批次当前采用全局串行。新文章不能追加到已确认批次；暂停只在当前请求明确返回后阻止下一笔，不强行中断在途请求。
- 服务商只有在返回明确成功和订单号时才建立本地订单。显式远端拒绝分为文章或媒体资源级错误与账号、余额或服务级错误，前者只处理当前文章，后者暂停剩余批次。
- 网络中断、超时或缺少订单号一律视为创建结果不确定。系统不得自动重试；文章进入需处理并保持冻结。补录订单号必须查询远端并尽量核对媒体资源、标题和系统投稿标识码。
- 网站媒体订单创建的正常 success、补录订单和“确认没有订单”共享唯一内部 attempt guard。可信带订单号成功高于人工“没有订单”；同一 attempt 出现不同可信订单号，或人工解冻后已有不兼容新目标时，保留订单安全证据并冻结为冲突，不自动挑选、覆盖目标或再次下单。
- 系统投稿标识码是用户配置的全局固定值，所有新网站媒体订单共用。修改只影响新订单，订单快照保留实际使用值；未配置时禁止下单。内部发布尝试继续使用独立唯一身份，不能把全局标识码当作幂等键。
- 网站媒体 API 适配层只负责媒体资源、投稿、订单详情、申诉和取消等供应商契约；业务状态映射、批次暂停、文章冻结和人工核对属于应用层。供应商 HTTP、字段命名和状态码不得泄漏到文章管理界面。
- 当前服务商网站媒体投稿字段为 API key、媒体资源 ID、标题、HTML 正文、可选备注和系统投稿标识；成功返回订单号。文档没有声明服务端幂等，也没有按系统投稿标识查询订单的契约，因此未知结果必须人工核对。
- 网站媒体订单状态映射为：`0 待安排`、`1 已安排`、`2 已发布`、`4 已退稿`、`9 售后中`。文章管理只使用付费处理中、需处理和已发布三个高层结果，详细状态留在订单页。
- 订单页默认筛选待安排，提供待安排、已安排、已发布、已退稿、售后中和全部；各筛选显示数量并按创建时间倒序。页面打开自动刷新一次，顶部刷新全部，详情刷新单个订单。
- 订单刷新、取消或申诉发生传输异常时保留原有远端事实，不猜测成功或失败。已知订单查询缺失进入状态异常并冻结，直到基于订单身份、最新 observation 与可核对证据完成人工收口；只允许恢复订单跟踪、确认已发布或确认明确非发布终态三类具名结果，证据不足继续冻结。
- 待安排显示取消订单；已安排显示尝试取消并提示可能被拒绝；已发布、已退稿和售后中不提供取消。明确取消成功且订单转换 guard 确认不存在全局已发布事实后恢复文章编辑；若取消在途期间已发布，取消只追加证据，不恢复编辑，历史订单永久保留。
- 已发布后的售后、退款、未收录、链接失效或结果不一致不撤销文章的全局已发布事实。用户需要再次铺设时必须创建全新文章。
- 已发布档案使用版本化 `publicationEvidenceV1` 保存实际提交正文（或明确不可得标记）、标题、content fingerprint、图片安全 fingerprint/布局/实际交付摘要或历史不可得标记、客户安全快照、目标、`submittedAt/submittedAtSource`、`firstPublishedAt/firstPublishedAtSource`、发布结果、订单号和安全远端链接。09 是该封闭 schema 与 validator 的唯一 owner，15、22、23 必须复用而不得平行解释。普通平台内容只来自 submission-start 同事务冻结的 `preparedSubmissionEvidenceV1`，不得回退 admission 原文或重建已丢失的浏览器会话；网站媒体内容来自订单创建不可变快照。首次发布时间优先可信远端事件，其次首次正面 observation，人工确认使用明确标记的 `manual_positive_evidence_time`。历史缺失使用 `null + legacy_unavailable + missing reason`，不得伪造，也不得以空图片清单冒充未知历史图片事实。界面称其为投稿内容，不抓取网页覆盖。
- 回收站只接收未发布且没有活动队列、订单或不确定结果的文章。永久删除正文后仍保留订单、标题、客户、目标、金额、时间和结果等最小审计信息。
- 订单保存媒体名称、资源 ID、确认单价、预计费用、系统投稿标识和文章标题快照；资源列表变更不改写历史。服务商若返回实际金额则单独记录。
- 旧数据迁移时忽略审核状态。workspace/schema gate 在正常 composition 前独占迁移，并以同一 operational SQLite migration metadata 中的持久 `MigrationJournalV1` 管理 `detected → backed_up → confirmed → import_committed → verified`；import 事实、schema/version 与 import_committed 同事务，正常 composition 只认可匹配当前稳定 workspace identity/source/version 的 verified journal。import 后验证前崩溃只重跑验证，不重复 import或因 schema 当前而放行。migration root 不构造任何远端能力。`ImportPlanV1` envelope、公共 entry 和六种 variant payload 全部版本化封闭、递归拒绝 extra fields/未知 enum；六种 variant 为 `publishedEvidence`、`trackablePaidOrder`、`pendingReadmission`、`nonPublishedTerminal`、`needsAttentionConflict`、`deletionRecoveryConflict`，均不得产生 runnable 事实或自动远端动作。`nonPublishedTerminal` 由 `closedTargetV1` 唯一承载跨渠道非发布终态；只有存在真实订单身份时才允许用 `orderHistoryV1` 对象承载订单 observation/terminal history，否则必须为 `null`，不得另行强制或伪造 `terminalObservationV1`。
- 模块边界按深模块设计：文章分类、编辑权限、队列编排、普通平台执行、网站媒体付费批次、订单同步与人工核对、图片库与图片准备、供应商适配分别拥有清晰职责。界面仅消费应用级命令和只读模型，不包含业务状态机；composition 只向应用服务注入其职责所需的最小具名持久化/外部能力，不把完整 OperationalStore 或供应商对象作为万能依赖传播。
- 每个模块提供少量稳定接口并封装状态转换、幂等和错误分类；不得以一个超长服务或页面组件承载全部流程。跨模块交互使用文章身份、目标身份、订单号和明确结果对象，避免共享可变结构；持久化 capability 只暴露调用方所需的具名事务，不传播完整 store。
- 图片库归属于客户，支持 JPG、PNG 和 WebP，允许子目录。生产普通平台图片源必须显式收窄到客户专用图片子目录；损坏或不支持文件跳过。同篇不重复、跨篇可复用，数量范围 0 至 5、默认 1。
- 图片扩展启用前已经存在、且没有 `imageCount` 字段的普通平台队列组必须迁移为 0，继续保持原纯文本确认结果；只有图片扩展启用后新建的组默认 1。旧组只有在用户明确修改并重新确认后才能启用图片，追加文章继承组当前配置。
- 图片准备在文章真正开始投稿时发生。每篇都从文章所属客户的专用图片目录当前可用集合完全随机选择；同篇不重复、跨篇可重复。请求数量不足时使用实际数量，无图或任意图片读取/准备失败时自动减量直至纯文本。Ticket 18 不拥有通用均匀插图算法，图片位置/图集/封面语义由各平台 adapter 按真实能力决定。
- 普通平台图片交付继续使用 08 的两阶段 `PreparedSubmission` 边界：提交前先完成文字与 best-effort 图片准备，图片失败不得生成新的用户 decision、文章失败或组阻塞；既有 V1 `decisionKind` 为历史兼容保留，但新自动路径固定 `initial`。ready 携带的 `PreparedSubmission` 是仅进程内窄 capability，公开 safe manifest 与唯一具名 submit，平台 session/token 隐藏在内部且禁止序列化、日志、IPC 或跨平台传递。executor 原子冻结 manifest 并写 submission-start 后才调用 submit；submit 不得再改变标题、正文或最终成功图片集合，内部状态漂移只能 uncertain。最终阶段仍只返回 accepted、article_rejected、group_blocked、uncertain。
- 网站媒体图片的业务规则已经确定，但传输机制尚未确定。当前桌面端预览和真实付费提交都只提取 DOCX 文字，不能用于带图验证；不得把“预览未显示但远端可能带图”作为实现假设。

## Testing Decisions

- 自动化测试只验证外部可观察行为，不断言内部函数、私有字段、文件布局或组件实现。真实服务商、真实账号和真实付费订单不得出现在自动化测试中。
- 最高层测试接缝限定为三个：文章管理分类与可执行动作投影、投稿队列应用命令、网站媒体订单状态转换与人工核对。只有供应商 multipart/HTTP 契约使用更低层合同测试。
- 文章分类测试覆盖六个互斥入口、标签计数、编辑权限和所有优先级组合，特别覆盖发布成功高于退稿、活动订单高于待投稿、未知结果始终冻结。
- 投稿资格测试覆盖无审核直接待投稿、标题或正文缺失、生成来源缺失但正文完整、活动目标、已发布和回收站。
- 普通平台编排测试覆盖平台与账号分组、不同平台跨组并行、同平台账号组串行及账号核验、同组 FIFO、追加到队尾、开始/暂停、prepared manifest 与 submission-start 同事务、边界前后崩溃、进程内 capability 消失后的证据恢复、重启暂停和平台独立完成。
- 普通平台结果测试覆盖接受即发布、文章失败继续、平台失败暂停、不确定结果 prepare 与两种证据绑定人工确认、stale/相反决定、迟到 accepted 成功优先和直接/人工成功时间来源；断言 08 是 intent/submission-start 唯一 writer、09 只标记/收口原 attempt，且不发生公开页面轮询。
- 网站媒体付费批次测试覆盖付费确认、开始全部排除、价格变化失效、媒体不可用、全局标识必填、严格串行、新文章不追加、两类明确拒绝的原子 outcome、正常成功/补录/确认无订单三方竞态、暂停后续提交和重启不自动恢复。
- 服务商合同测试使用内存传输捕获请求，验证网站媒体接口字段、HTML 正文、全局投稿标识、订单号解析、取消和订单详情；所有响应均为假数据。
- 订单映射测试覆盖状态 0、1、2、4、9，明确取消、取消拒绝、查询缺失及三类人工收口、人工确认发布的时间来源、刷新失败、长期未完成提示、售后与退款不撤销已发布；取消在途期间并发观察到发布成功时，永久发布事实优先且不得恢复编辑。
- 未知订单测试覆盖超时、断线、成功响应缺少订单号、稳定 `orderCreationAttemptId`、禁止自动重试、手工补录验证、错误订单拒绝绑定、不同 attempt 竞争同一订单号和人工确认无订单后按阻塞事实决定是否解冻。
- 历史与删除测试覆盖投稿正文快照、媒体和价格快照、订单永久保留、回收站恢复、永久删除正文后订单仍可查询、已发布不能回收。
- Renderer 测试覆盖六个入口、队列精简展示、默认订单筛选、刷新按钮、付费确认、取消提示、需处理允许动作以及窄宽度布局，不复制领域状态判断。
- 迁移测试覆盖 journal 每个 phase、import commit 后立即崩溃、备份复用/失效、验证重跑、六种封闭 payload、extra fields、未知 enum、重复文章/订单身份、正文/时间缺失和回收冲突。schema 当前但 journal 未 verified 必须阻断；`importLifecycleFacts` 拒绝任何 runnable 事实，migration root 不构造远端能力，失败不写部分事实。
- 性能测试重点验证分类投影和导航计数在大量文章、队列和订单下仍使用批量读取，不发生逐文章磁盘或数据库查询。手机号和网址风险检测只扫描本次确认内容。
- 图片库测试覆盖客户隔离、完全随机数量、同篇不重复/跨篇可复用、损坏文件、数量不足和零图；生产图片源必须显式收窄到客户专用图片子目录。普通平台图片合同覆盖核心旧组迁移为 0、新组默认 1、配置继承/修改、claim-time 选图、best-effort 自动降级、safe manifest、进程内 capability 与 submission-start/submit 顺序；新图片路径不建立 retry/换图/人工降级 decision 状态机，既有 V1 `decisionKind` 仅为历史兼容且自动路径固定 `initial`。图片具体位置/图集/封面语义由各平台 adapter 依据真实能力拥有。网站媒体图片传输合同测试必须等真实低价媒体验证出传输方式和限制后再编写。
- 核心里程碑的手工验收至少覆盖普通平台纯文本两组同时运行和网站媒体真实订单状态刷新；真实付费验证必须由用户明确确认并记录订单号、媒体资源、最终链接和结果。图片专项验证移至核心完成后的独立图片扩展/探索阶段，不是核心完成门槛。普通平台必须逐个先以本次明确授权完成真实能力探索并输出 `SUPPORTED|UNSUPPORTED|INCONCLUSIVE`；只有 `SUPPORTED` 进入 adapter 实施，实施后的真实带图投稿验收必须再次获得该平台的单独明确授权。前置探索授权不自动延伸为实施或验收授权。
- 普通平台图片 adapter 的自动化只使用假页面/假运行时；仅对前置真实探索结论为 `SUPPORTED` 的平台实施 Ticket 19/20/21。对应 adapter 实施并合并后，图片扩展里程碑还必须由用户对该平台另行明确授权真实带图投稿，记录安全目标身份、图片格式/尺寸/数量、提交边界、结果和停止条件。缺少任一已实施平台的验收证据时只能标记实现待外部验收；对已有合规 `UNSUPPORTED` / `INCONCLUSIVE` 结论的平台，保持图片入口关闭即可收口。
- 优先复用现有文章管理快照、投稿资格、发布工作流、网站媒体传输、订单投影、Renderer 文章管理和队列控制测试模式；新增接缝只允许出现在集中分类器或供应商图片适配边界。

## Out of Scope

- 第三方自媒体的资源列表、投稿、订单、取消和申诉。
- 人工审核、批量审核或任何审核状态兼容入口；未来如需要，应作为独立前置模块另行设计。
- 同一篇文章同时投向多个普通平台、多个网站媒体资源或普通与付费目标并存。
- 普通平台接受投稿后的公开页面检测、审核轮询、收录查询和可见性回查。
- 复制为新版本、复制为新文章和已发布文章再次投稿。
- 跨文章相似度拦截、AI 查重和自动重写。
- 网站媒体自动申诉、自动取消、自动确认订单不存在或自动重试未知订单。
- 已发布文章进入回收站或删除发布与订单证据。
- 自建 OSS、外部图片托管服务或依赖另一个系统的私有 UEditor 上传接口。
- 在未完成真实验证前实现网站媒体本地图片传输、承诺支持的图片大小或承诺最大请求体。

## Further Notes

- 服务商网站媒体接口文档确认存在媒体资源列表、投稿、订单详情、申诉、媒体分类和取消订单；当前范围不读取或实现第三方自媒体接口。
- 网站媒体投稿 API 接收 HTML 正文但没有公开图片上传字段。另一个对接系统会先把本地图片上传到自己的 UEditor 路径，再把公开地址插入正文；这只能证明网站媒体业务支持图片，不能证明服务商 API 接受本地路径或内嵌图片。
- 当前桌面端付费媒体的文章预览与实际提交都从 DOCX 提取纯文字并重新生成 HTML，DOCX 图片不会随现有流程发送。因此现有界面不能作为网站媒体图片能力测试工具。
- 网站媒体图片待实测项和后置的普通平台图片扩展都不影响核心文章生命周期、纯文本队列、订单、迁移、删除和分类的完成。核心模块必须保留封闭 evidence 接缝与独立供应商适配边界；真实验证后仍需另建实施 ticket，不能把探索证据直接当成功能完成。
- 旧规则“待审核/已审核”“同一文章多个发布目标”“投稿队列副本”“通用已提交”“已发布文章可回收”均被本规格明确废止，不应作为兼容业务继续出现在界面或新模块接口中。
- 本规格是后续任务拆分和各实施分支的共同基线。实施分支应从包含本规格和权威词汇表的提交创建，避免每个分支复制或改写业务规则。
