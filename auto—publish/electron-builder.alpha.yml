appId: com.autopublish.desktop
productName: 鱼饼大王
copyright: "Copyright © 2026"

directories:
  app: .
  output: release-alpha
  buildResources: build

files:
  # Electron main process
  - desktop/**/*
  # Business logic (adapters, core, platforms)
  - src/**/*
  # Config defaults (platforms.json)
  - config/**/*
  # Runtime scripts required by adapters (config.js, etc.)
  - scripts/**/*
  # One-shot data migration is an operator tool, never an installed app asset.
  - "!scripts/migrate-content-library-v2.js"
  - "!scripts/migrate-content-metadata-v1.js"
  - "!scripts/migrate-operational-store-v1.js"
  # React production build (generated by build:renderer)
  - media-workbench/dist/**/*
  # Adapter runtime helpers (Python scripts, etc.)
  - src/platforms/hepan/hepan_publish.py
  # Read-only builtin content templates
  - resources/content-templates/**/*
  # Portable Python dependencies for the Hepan adapter; the settings UI still allows a custom override.
  - resources/hepan/requirements.txt
  - resources/hepan/vendor-pure/**/*
  - "!resources/hepan/vendor-pure/bin/**"
  - "!resources/hepan/vendor-pure/**/*.pyc"
  - "!resources/hepan/vendor-pure/**/__pycache__/**"
  # Do not list node_modules here. electron-builder derives the complete
  # production dependency closure from package.json/package-lock, including
  # transitive dependencies required by mammoth and form-data.
  # Exclude dev/cache noise
  - "!node_modules/.cache/**"
  - "!**/*.map"
  - "!**/*.tsbuildinfo"

  # ---- NEVER BUNDLE PRIVATE RUNTIME DATA ----
  - "!**/.env"
  - "!**/ai-provider.json"
  - "!**/media-provider.json"
  - "!**/hepan-provider.json"
  - "!**/platform-settings-migration.json"
  - "!**/provider-test-status.json"
  - "!**/.hepan-cookie-*.tmp"
  - "!**/.hepan-images-*.tmp"
  - "!**/workspace-location.json"
  - "!input/**"
  - "!data/**"
  - "!**/content-generation-batches/**"
  - "!logs/**"
  - "!**/doubao-diagnostics/**"
  - "!published/**"
  - "!failed/**"
  - "!tmp/**"
  - "!work/**"
  - "!**/client-material-cache/**"
  - "!**/.autopublish/**"
  - "!**/submission-records/**"
  - "!**/publications/**"
  - "!.playwright-cli/**"
  - "!browser/**"
  - "!**/browser/**"
  - "!research/**"
  - "!**/research/**"
  - "!clients/**"
  - "!generated/**"
  - "!**/generated/**"
  - "!tests/fixtures/**"
  - "!**/tests/fixtures/**"
  # Repo metadata
  - "!docs/**"
  - "!.git/**"
  - "!release-alpha/**"

# Only child-process runtimes that cannot load Electron ASAR are unpacked.
# All application modules remain in app.asar.
asar: true
asarUnpack:
  - src/platforms/hepan/hepan_publish.py
  - resources/hepan/vendor-pure/**/*
  - node_modules/@playwright/cli/**/*
  - node_modules/playwright/**/*
  - node_modules/playwright-core/**/*

# Node itself is an executable resource, not an application module. It must
# remain outside app.asar so the packaged CLI can be launched directly.
extraResources:
  - from: build/runtime-tools/node
    to: tools/node
    filter:
      - "**/*"

win:
  # Alpha artifacts are intentionally unsigned; production signing is a release-pipeline concern.
  signExecutable: false
  target:
    - target: portable
      arch:
        - x64
    - target: nsis
      arch:
        - x64
  artifactName: "${productName}-Alpha-${version}-${arch}.${ext}"

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: 鱼饼大王 Alpha

portable:
  artifactName: "${productName}-Alpha-${version}-portable.${ext}"
