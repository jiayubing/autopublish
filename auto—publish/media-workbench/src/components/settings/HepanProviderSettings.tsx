import React, { useEffect, useState } from 'react';
import { clearPlatformSettings, getPlatformSettingsStatus, savePlatformSettings, testPlatformSettings } from '../../electron-api';
import type { HepanProviderStatus } from '../../types';

const EMPTY: HepanProviderStatus = { source: 'application', configured: false, pythonConfigured: false, cookieConfigured: false, categoryId: 121, vendorConfigured: false, siteOrigin: 'https://www.hepan.com', lastTest: null };
function message(error: unknown): string { const code = error && typeof error === 'object' && 'code' in error ? String((error as { code?: unknown }).code || '') : ''; return ({ PLATFORM_CONFIG_INVALID: '配置无效，请检查 Python 路径、Cookie 和栏目 ID。', PLATFORM_CONFIG_BUSY: '蓝色河畔投稿运行中，暂时不能修改配置。', PLATFORM_CONFIG_ENV_OVERRIDE: '配置由环境变量覆盖，当前页面只能查看。', HEPAN_PYTHON_UNAVAILABLE: 'Python 不可用，请检查可执行文件。', HEPAN_DEPENDENCY_MISSING: 'Python 依赖缺失，请检查 vendor 目录或环境。', HEPAN_LOGIN_INVALID: 'Cookie 登录检查失败，请更新 Cookie。' } as Record<string, string>)[code] || '蓝色河畔配置操作失败。'; }

export default function HepanProviderSettings() {
  const [status, setStatus] = useState(EMPTY); const [pythonPath, setPythonPath] = useState(''); const [cookie, setCookie] = useState(''); const [categoryId, setCategoryId] = useState(121); const [vendorDir, setVendorDir] = useState(''); const [loading, setLoading] = useState(true); const [busy, setBusy] = useState(false); const [error, setError] = useState(''); const [notice, setNotice] = useState('');
  const load = async () => { try { const next = await getPlatformSettingsStatus<HepanProviderStatus>('hepan'); setStatus(next); setCategoryId(next.categoryId || 121); } catch (value) { setError(message(value)); } finally { setLoading(false); } };
  useEffect(() => { void load(); }, []);
  const draft = () => ({ pythonPath: pythonPath.trim(), cookie, categoryId: Number(categoryId), vendorDir: vendorDir.trim() });
  const validate = () => { if (!pythonPath.trim() && !status.pythonConfigured) { setError('请输入 Python 可执行文件路径。'); return false; } if (!cookie.trim() && !status.cookieConfigured) { setError('请输入河畔 Cookie。'); return false; } return true; };
  const save = async () => { setError(''); setNotice(''); if (!validate()) return; setBusy(true); try { setStatus(await savePlatformSettings('hepan', draft())); setCookie(''); setNotice('蓝色河畔配置已保存，下一次任务立即生效。'); } catch (value) { setError(message(value)); } finally { setBusy(false); } };
  const test = async () => { setError(''); setNotice(''); if (!validate()) return; if (typeof window !== 'undefined' && !window.confirm('测试会检查 Python、依赖和 Cookie 登录，不会发布文章。是否继续？')) return; setBusy(true); try { await testPlatformSettings('hepan', draft()); setNotice('Python、依赖和登录检查成功。'); await load(); } catch (value) { setError(message(value)); await load(); } finally { setBusy(false); } };
  const clear = async () => { setError(''); setNotice(''); if (typeof window !== 'undefined' && !window.confirm('清除蓝色河畔配置后将无法投稿。是否继续？')) return; setBusy(true); try { await clearPlatformSettings('hepan'); setStatus(EMPTY); setCookie(''); setNotice('蓝色河畔配置已清除。'); } catch (value) { setError(message(value)); } finally { setBusy(false); } };
  const readOnly = status.source === 'environment';
  return <section aria-labelledby="hepan-provider-settings-title" className="space-y-4 rounded-lg border border-slate-200 bg-white p-5"><div><h3 id="hepan-provider-settings-title" className="text-base font-semibold text-slate-800">蓝色河畔</h3><p className="mt-1 text-sm text-slate-500">应用级发文运行环境。Cookie 加密保存，只在测试或任务期间解密。</p></div><div className="rounded-md border border-blue-100 bg-blue-50 p-3 text-xs text-blue-900">站点：{status.siteOrigin} · 配置来源：{readOnly ? '环境变量覆盖（只读）' : '应用级加密存储'}</div><div className="grid gap-3"><label className="grid min-w-0 gap-1 text-sm font-medium text-slate-700">Python 可执行文件<input value={pythonPath} onChange={(event) => setPythonPath(event.target.value)} placeholder="C:\Python312\python.exe" disabled={busy || loading || readOnly} className="min-w-0 rounded-md border border-slate-300 px-3 py-2 text-sm" /></label><label className="grid min-w-0 gap-1 text-sm font-medium text-slate-700">河畔 Cookie<input type="password" value={cookie} onChange={(event) => setCookie(event.target.value)} placeholder={status.cookieConfigured ? '已配置（留空保留）' : '请输入 Cookie'} autoComplete="new-password" disabled={busy || loading || readOnly} className="min-w-0 rounded-md border border-slate-300 px-3 py-2 text-sm" /></label></div><details className="rounded-md border border-slate-200 p-3"><summary className="cursor-pointer text-sm font-medium text-slate-700">高级配置</summary><div className="mt-3 grid gap-3 sm:grid-cols-[12rem_minmax(0,1fr)]"><label className="grid gap-1 text-sm text-slate-600">栏目 ID<input type="number" min={1} step={1} value={categoryId} onChange={(event) => setCategoryId(Number(event.target.value))} disabled={busy || loading || readOnly} className="min-w-0 rounded-md border border-slate-300 px-3 py-2 text-sm" /></label><label className="grid min-w-0 gap-1 text-sm text-slate-600">Python vendor/依赖目录<input value={vendorDir} onChange={(event) => setVendorDir(event.target.value)} disabled={busy || loading || readOnly} className="min-w-0 rounded-md border border-slate-300 px-3 py-2 text-sm" /></label></div></details><p className="text-xs text-slate-500">Python：{status.pythonConfigured ? '已配置' : '未配置'} · Cookie：{status.cookieConfigured ? '已配置' : '未配置'} · 最近测试：{status.lastTest ? (status.lastTest.ok ? '成功' : '失败') : '尚未测试'}</p>{error && <p role="alert" aria-live="polite" className="text-sm text-rose-700">{error}</p>}{notice && <p role="status" aria-live="polite" className="text-sm text-emerald-700">{notice}</p>}<div className="flex flex-wrap gap-2"><button type="button" onClick={() => void save()} disabled={busy || loading || readOnly} className="rounded-md bg-blue-600 px-3 py-2 text-sm font-semibold text-white disabled:opacity-50">保存配置</button><button type="button" onClick={() => void test()} disabled={busy || loading || readOnly} className="rounded-md border border-slate-300 px-3 py-2 text-sm disabled:opacity-50">测试登录</button><button type="button" onClick={() => void clear()} disabled={busy || loading || readOnly} className="rounded-md border border-rose-300 px-3 py-2 text-sm text-rose-700 disabled:opacity-50">清除配置</button></div></section>;
}
